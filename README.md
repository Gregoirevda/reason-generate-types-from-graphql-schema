# reason-generate-types-from-graphql-schema

This is work in progress

This package generates Reason types from a remote graphql server

`ocamlbuild -r -use-ocamlfind  src/index.native`
`./index.native`